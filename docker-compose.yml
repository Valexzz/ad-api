services:
  ad-api:
    volumes:
      - ./config.yml:/app/config.yml
    image: ad_api
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ad_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env